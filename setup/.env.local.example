# =================================================
# CONFIGURATION AMASO - FRONTEND NEXT.JS
# Interface Utilisateur du Système de Gestion Amaso
# =================================================
#
# Ce fichier contient les variables d'environnement pour le frontend
# de l'application Amaso développée avec Next.js.
#
# INSTRUCTIONS D'UTILISATION :
# 1. Copiez ce fichier et renommez-le en ".env.local"
# 2. Modifiez les valeurs selon votre configuration
# 3. Redémarrez le serveur de développement
#
# ⚠️ IMPORTANT : Les variables commençant par NEXT_PUBLIC_ 
# sont accessibles côté navigateur !
#

# =================================================
# CONNEXION À L'API BACKEND
# =================================================

# URL de base de l'API Laravel
# Cette URL doit pointer vers votre serveur backend
# Développement local : http://localhost:8000/api/v1
# Production : https://votre-domaine.com/api/v1
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# URL de base du backend (sans /api/v1)
# Utilisée pour les fichiers, images, documents
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# =================================================
# CONFIGURATION DE L'ENVIRONNEMENT
# =================================================

# Environnement d'exécution
# development = développement (logs détaillés, hot-reload)
# production = production (optimisé, logs minimaux)
NODE_ENV=development

# =================================================
# CONFIGURATION NEXT.JS
# =================================================

# URL publique de l'application frontend
# Utilisée pour les redirections, partages, etc.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Nom de l'application (affiché dans l'interface)
NEXT_PUBLIC_APP_NAME="Système de Gestion Amaso"

# Version de l'application (optionnel)
NEXT_PUBLIC_APP_VERSION="1.0.0"

# Désactiver la télémétrie Next.js (collecte de données anonymes)
# 1 = désactivé, 0 = activé
NEXT_TELEMETRY_DISABLED=1

# =================================================
# CONFIGURATION RÉGIONALE
# =================================================

# Langue par défaut de l'interface
# ar = arabe, fr = français, en = anglais
NEXT_PUBLIC_DEFAULT_LOCALE=ar

# Direction du texte (pour le CSS)
# rtl = droite à gauche (arabe), ltr = gauche à droite
NEXT_PUBLIC_TEXT_DIRECTION=rtl

# Configuration du calendrier
# hijri = calendrier hégirien, gregorian = calendrier grégorien
NEXT_PUBLIC_CALENDAR_TYPE=hijri

# =================================================
# FONCTIONNALITÉS DE L'APPLICATION
# =================================================

# Activer/désactiver certaines fonctionnalités
# true = activé, false = désactivé

# Mode démonstration (données factices)
NEXT_PUBLIC_DEMO_MODE=false

# Fonctionnalité d'export des données
NEXT_PUBLIC_ENABLE_EXPORT=true

# Fonctionnalité d'impression des rapports
NEXT_PUBLIC_ENABLE_PRINT=true

# Notifications temps réel
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true

# =================================================
# CONFIGURATION DE L'INTERFACE
# =================================================

# Thème par défaut
# light = clair, dark = sombre, auto = selon le système
NEXT_PUBLIC_DEFAULT_THEME=light

# Couleur principale de l'interface (optionnel)
# Utilisée pour personnaliser les couleurs
NEXT_PUBLIC_BRAND_COLOR="#10b981"

# Logo de l'organisation (chemin vers l'image)
NEXT_PUBLIC_LOGO_URL="/images/amaso-logo.png"

# =================================================
# CONFIGURATION DE SÉCURITÉ
# =================================================

# Domaines autorisés pour les images externes
# Sécurise le chargement d'images externes
NEXT_PUBLIC_ALLOWED_IMAGE_DOMAINS="localhost,amaso.org"

# Durée de session en minutes
# Après ce délai, l'utilisateur sera déconnecté automatiquement
NEXT_PUBLIC_SESSION_TIMEOUT=120

# =================================================
# SERVICES EXTERNES (OPTIONNEL)
# =================================================

# Google Analytics (pour mesurer l'utilisation)
# Laissez vide si vous ne l'utilisez pas
NEXT_PUBLIC_GA_TRACKING_ID=

# Service de cartes géographiques
# google = Google Maps, openstreetmap = OpenStreetMap
NEXT_PUBLIC_MAP_PROVIDER=openstreetmap

# Clé API Google Maps (si utilisé)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# =================================================
# CONFIGURATION DE DÉVELOPPEMENT
# =================================================

# Port du serveur de développement (optionnel)
# Par défaut : 3000
PORT=3000

# URL du serveur de développement
DEV_SERVER_URL=http://localhost:3000

# Activer les logs de debug dans la console
NEXT_PUBLIC_DEBUG_MODE=true

# Afficher les informations de performance
NEXT_PUBLIC_SHOW_PERFORMANCE=false

# =================================================
# INTÉGRATIONS SPÉCIFIQUES AMASO
# =================================================

# Configuration pour les rapports financiers
NEXT_PUBLIC_FINANCIAL_YEAR_START=1  # 1 = janvier (mois de début d'exercice)

# Format de devise par défaut
NEXT_PUBLIC_DEFAULT_CURRENCY=SAR    # SAR = Riyal saoudien, USD = Dollar

# Format des montants
NEXT_PUBLIC_CURRENCY_DECIMAL_PLACES=2

# Pagination par défaut
NEXT_PUBLIC_DEFAULT_PAGE_SIZE=25

# =================================================
# CONFIGURATION DE PRODUCTION
# =================================================
#
# POUR LA MISE EN PRODUCTION, MODIFIEZ :
# - NODE_ENV=production
# - NEXT_PUBLIC_API_BASE_URL=https://votre-api.com/api/v1
# - NEXT_PUBLIC_APP_URL=https://votre-domaine.com
# - NEXT_PUBLIC_DEBUG_MODE=false
# - Ajoutez les vrais domaines dans ALLOWED_IMAGE_DOMAINS
#

# =================================================
# NOTES IMPORTANTES
# =================================================
#
# VARIABLES PUBLIQUES :
# - Toute variable commençant par NEXT_PUBLIC_ est accessible
#   côté navigateur (client)
# - Ne jamais y mettre de secrets ou mots de passe !
#
# VARIABLES PRIVÉES :
# - Les autres variables ne sont accessibles que côté serveur
# - Parfait pour les clés API, secrets, etc.
#
# REDÉMARRAGE REQUIS :
# - Après modification de ce fichier, redémarrez le serveur :
#   npm run dev (développement) ou npm run build + npm start (production)
#
# SÉCURITÉ :
# - Ne jamais commiter ce fichier dans un dépôt public
# - Gardez une copie de sauvegarde sécurisée
# - En production, utilisez toujours HTTPS
#