# Dependencies
node_modules/
**/node_modules/

# Production builds
frontend/.next/
frontend/out/
frontend/build/
frontend/dist/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*/.env
*/.env.*

# Allow environment templates to be committed
!.env.example
!.env.*.example
!*/.env.example
!*/.env.*.example

# Database
*.sqlite
*.sqlite3
*.db
backend/database/database.sqlite

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
backend/storage/logs/*.log

# Laravel specific
backend/vendor/
backend/storage/app/public/
# storage/framework/{cache,sessions,testing,views}, storage/logs and
# bootstrap/cache each carry their own tracked .gitignore (Laravel's stock
# `*` + `!.gitignore` placeholder) - that's what keeps the directory itself
# in git while still ignoring the runtime files inside it. A blanket
# `backend/storage/framework/views/` rule here excluded that placeholder
# too, so a fresh clone never got the directory at all: Blade has nowhere
# to write compiled views, and every PDF export (all of them render a
# Blade template) failed with "Please provide a valid cache path."
# regardless of what request or filters produced it.
backend/.phpunit.result.cache
backend/Homestead.json
backend/Homestead.yaml
backend/.vagrant
backend/.env.backup
backend/.env.production
backend/.phpunit.cache

# Composer
# composer.lock is deliberately NOT ignored. This is an application, not a
# library: the lock is what makes `composer install` reproduce the same
# dependency set everywhere. While it was ignored, adding mpdf and
# phpspreadsheet shipped the composer.json entry without the packages, and
# every PDF and Excel export returned a 500 on machines that had not
# happened to install them by hand.
backend/auth.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
jspm_packages/

# TypeScript v1 declaration files
typings/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt

# Gatsby files
.cache/

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Editor directories and files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# PHP Storm
.idea/

# Sublime Text
*.sublime-workspace
*.sublime-project

# VS Code
.vscode/settings.json
.vscode/tasks.json
.vscode/launch.json
.vscode/extensions.json

# Development files
*.todo
*.notes
scratch/
sandbox/

# Local deployment folder - allow pushing to repository
!local/

# Backup files
*.bak
*.backup
*~

# Archive files
*.zipI trried 
*.tar.gz
*.rar

# Cache files
.cache/
*.cache

# Test files
test-results/
coverage/

# Progress tracking (development files)
progress.md
ARCHITECTURE_DECISIONS.md
DEVELOPMENT_PROGRESS.md
CLAUDE.md
.claude/
settings.local.json

rapport/
backend\app\Providers\AppServiceProvider.php
